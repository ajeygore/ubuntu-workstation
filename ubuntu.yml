---
  - name: "Setting up Ubuntu as workstationt"
    hosts: localhost
    connection: local 
    tasks:

    - name: Install yadr packages
      package:
        name:
          - screen
          - software-properties-common
          - autoconf 
          - build-essential
          - curl
          - fasd
          - fontconfig
          - git
          - sudo
          - tmux
          - vim
          - wget
          - zsh
          - golang
          - autoconf
          - bison
          - libssl-dev
          - libyaml-dev
          - libreadline6-dev
          - zlib1g-dev
          - libncurses5-dev
          - libffi-dev
          - libgdbm6
          - libdb-dev

        state: present
